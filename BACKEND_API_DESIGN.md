# 前后端通信架构设计文档

## 1. 架构可行性分析

### 1.1 前后端分离架构模式 - ✅ 完全可行

```
┌─────────────────────────────┐
│   React/Vue 前端应用        │
│  (暗色主题 + 科技风格)       │
└──────────────┬──────────────┘
               │ HTTP/HTTPS
               │ JSON格式
               ▼
┌─────────────────────────────┐
│  Python 后端服务             │
│  (Flask/FastAPI)            │
│  ├─ 爬虫模块                 │
│  ├─ 数据处理模块             │
│  ├─ 分析计算模块             │
│  └─ 缓存/存储模块            │
└─────────────────────────────┘
```

### 1.2 核心优势

| 优势 | 说明 |
|-----|------|
| **解耦** | 前后端独立开发、部署、升级 |
| **可扩展** | 后端可轻松接入多个前端（web、app、小程序） |
| **异步处理** | 长时间爬虫任务可后台运行 |
| **缓存优化** | 减少重复爬虫，提升响应速度 |
| **并发能力** | 多个前端用户同时请求 |
| **实时反馈** | 可支持进度条、WebSocket实时更新 |

### 1.3 通信流程

```
前端触发        后端处理        数据返回        前端渲染
(用户点击)      (爬虫/分析)    (JSON)          (图表展示)
  │              │              │              │
  └─ 发送请求 ──▶ 执行业务逻辑 ──▶ 返回数据 ──▶ 状态管理
     {query}     处理异常      {result}      展示UI
```

---

## 2. API 接口设计规范

### 2.1 API 基础信息

| 属性 | 值 |
|-----|-----|
| **基础URL** | `http://localhost:5000/api/v1` (开发) |
| **版本号** | v1 |
| **内容类型** | `application/json` |
| **认证方式** | 暂无（可扩展为JWT） |
| **超时时间** | 30秒（爬虫任务60秒） |

### 2.2 响应格式规范

#### 成功响应 (200)
```json
{
  "code": 200,
  "message": "成功",
  "data": {
    // 实际数据
  },
  "timestamp": "2025-01-07T10:30:45Z"
}
```

#### 错误响应 (400/500)
```json
{
  "code": 400,
  "message": "错误描述",
  "error": {
    "type": "ValidationError",
    "details": "具体错误信息"
  },
  "timestamp": "2025-01-07T10:30:45Z"
}
```

#### 异步任务响应 (202)
```json
{
  "code": 202,
  "message": "任务已提交",
  "data": {
    "task_id": "task_12345",
    "status": "running",
    "progress": 25
  },
  "timestamp": "2025-01-07T10:30:45Z"
}
```

---

## 3. 核心API接口清单

### 3.1 爬虫执行接口

#### 📌 接口1：执行爬虫
```
POST /crawler/start
```

**请求体**：
```json
{
  "max_pages": 10,
  "timeout": 30,
  "async": true
}
```

**参数说明**：
| 参数 | 类型 | 必需 | 说明 |
|-----|------|------|------|
| max_pages | Int | 是 | 最大爬取页数 (1-50) |
| timeout | Int | 否 | 超时时间(秒)，默认30 |
| async | Bool | 否 | 是否异步执行，默认true |

**成功响应 (200)**：
```json
{
  "code": 200,
  "message": "爬虫执行成功",
  "data": {
    "total_questions": 456,
    "total_users": 89,
    "total_tags": 234,
    "questions": [
      {
        "id": "1",
        "title": "Python编程问题",
        "user": "用户1",
        "reputation": 150,
        "views": 1250,
        "likes": 45,
        "answers": 8,
        "tags": ["Python", "编程"],
        "asked_time": "2025-01-07",
        "precise_time": "2025-01-07T10:30:00Z"
      }
      // ... 更多问题
    ]
  },
  "timestamp": "2025-01-07T10:35:20Z"
}
```

**异步响应 (202)**：
```json
{
  "code": 202,
  "message": "爬虫正在运行",
  "data": {
    "task_id": "crawler_task_abc123",
    "status": "running",
    "progress": 30,
    "current_page": 3,
    "total_pages": 10,
    "message": "正在爬取第3页..."
  }
}
```

**错误响应 (400/500)**：
```json
{
  "code": 400,
  "message": "参数验证失败",
  "error": {
    "type": "ValidationError",
    "details": "max_pages 必须在 1-50 之间"
  }
}
```

---

#### 📌 接口2：查询爬虫任务状态
```
GET /crawler/task/{task_id}
```

**路径参数**：
| 参数 | 说明 |
|-----|------|
| task_id | 任务ID，来自start接口返回 |

**响应**：
```json
{
  "code": 200,
  "message": "任务信息获取成功",
  "data": {
    "task_id": "crawler_task_abc123",
    "status": "completed",
    "progress": 100,
    "total_questions": 456,
    "result": {
      // 完整的爬虫数据
    }
  }
}
```

---

#### 📌 接口3：停止爬虫任务
```
POST /crawler/stop/{task_id}
```

**响应**：
```json
{
  "code": 200,
  "message": "任务已停止",
  "data": {
    "task_id": "crawler_task_abc123",
    "status": "stopped",
    "questions_fetched": 123
  }
}
```

---

### 3.2 数据分析接口

#### 📌 接口4：获取仪表板数据
```
GET /analysis/dashboard
```

**查询参数**：
```
?use_cache=true&cache_ttl=3600
```

| 参数 | 说明 |
|-----|------|
| use_cache | 是否使用缓存，默认true |
| cache_ttl | 缓存过期时间(秒)，默认3600 |

**响应**：
```json
{
  "code": 200,
  "message": "仪表板数据获取成功",
  "data": {
    "basic_stats": {
      "total_questions": 456,
      "total_views": 45680,
      "total_likes": 2345,
      "total_answers": 1234,
      "total_reputation": 12450,
      "total_users": 89
    },
    "top_questions": [
      {
        "id": "1",
        "title": "...",
        "views": 1250,
        "likes": 45,
        "answers": 8,
        "time": "2025-01-07"
      }
      // Top 10
    ],
    "top_users": [
      {
        "user": "用户1",
        "question_count": 15,
        "total_views": 2100,
        "total_likes": 180,
        "reputation": 450
      }
      // Top 5
    ],
    "top_tags": [
      { "tag": "Python", "count": 45 },
      { "tag": "JavaScript", "count": 38 }
      // Top 15
    ]
  }
}
```

---

#### 📌 接口5：获取趋势数据
```
GET /analysis/trends
```

**查询参数**：
```
?start_date=2024-12-01&end_date=2025-01-07&granularity=monthly
```

| 参数 | 说明 | 示例 |
|-----|------|------|
| start_date | 开始日期 | 2024-12-01 |
| end_date | 结束日期 | 2025-01-07 |
| granularity | 粒度：daily/weekly/monthly | monthly |

**响应**：
```json
{
  "code": 200,
  "message": "趋势数据获取成功",
  "data": {
    "monthly": [
      {
        "month": "2024-12",
        "question_count": 145,
        "total_views": 15000,
        "total_likes": 850,
        "total_answers": 450
      },
      {
        "month": "2025-01",
        "question_count": 120,
        "total_views": 12000,
        "total_likes": 700,
        "total_answers": 380
      }
    ]
  }
}
```

---

#### 📌 接口6：获取用户分析
```
GET /analysis/users
```

**查询参数**：
```
?limit=10&sort_by=question_count
```

| 参数 | 说明 |
|-----|------|
| limit | 返回数量，默认10 |
| sort_by | 排序字段：question_count/total_views/reputation |

**响应**：
```json
{
  "code": 200,
  "message": "用户分析数据获取成功",
  "data": {
    "total_users": 89,
    "avg_questions_per_user": 5.1,
    "users": [
      {
        "rank": 1,
        "user": "数据分析师",
        "question_count": 15,
        "total_views": 2100,
        "total_likes": 180,
        "total_answers": 45,
        "reputation": 450
      }
      // 更多用户
    ]
  }
}
```

---

#### 📌 接口7：获取标签分析
```
GET /analysis/tags
```

**查询参数**：
```
?limit=15&include_co_occurrence=false
```

| 参数 | 说明 |
|-----|------|
| limit | 返回标签数 |
| include_co_occurrence | 是否包含标签共现数据 |

**响应**：
```json
{
  "code": 200,
  "message": "标签分析数据获取成功",
  "data": {
    "total_tags": 234,
    "tags": [
      { "rank": 1, "tag": "Python", "count": 45 },
      { "rank": 2, "tag": "JavaScript", "count": 38 }
      // 更多标签
    ]
  }
}
```

---

#### 📌 接口8：获取问题列表（分页）
```
GET /analysis/questions
```

**查询参数**：
```
?page=1&limit=20&sort_by=views&order=desc&search=Python
```

| 参数 | 说明 |
|-----|------|
| page | 页码，从1开始 |
| limit | 每页数量 |
| sort_by | 排序字段 |
| order | asc/desc |
| search | 搜索关键词 |
| tags | 标签过滤(逗号分隔) |
| min_views | 最小浏览数 |

**响应**：
```json
{
  "code": 200,
  "message": "问题列表获取成功",
  "data": {
    "total": 456,
    "page": 1,
    "limit": 20,
    "pages": 23,
    "questions": [
      {
        "id": "1",
        "title": "...",
        "user": "...",
        "reputation": 150,
        "views": 1250,
        "likes": 45,
        "answers": 8,
        "tags": ["Python"],
        "time": "2025-01-07"
      }
      // 20条问题
    ]
  }
}
```

---

#### 📌 接口9：数据导出
```
POST /analysis/export
```

**请求体**：
```json
{
  "format": "csv",
  "data_type": "questions",
  "filters": {
    "tags": ["Python"],
    "min_views": 100
  }
}
```

| 参数 | 说明 | 可选值 |
|-----|------|-------|
| format | 导出格式 | csv/excel/json |
| data_type | 导出数据类型 | questions/users/tags/all |

**响应**：
```json
{
  "code": 200,
  "message": "数据导出成功",
  "data": {
    "file_url": "/download/export_20250107_103045.csv",
    "file_name": "export_20250107_103045.csv",
    "size": "2.5MB",
    "created_at": "2025-01-07T10:30:45Z"
  }
}
```

---

#### 📌 接口10：获取缓存状态
```
GET /system/cache-status
```

**响应**：
```json
{
  "code": 200,
  "message": "缓存状态获取成功",
  "data": {
    "cache_enabled": true,
    "cache_size": "45.2MB",
    "cache_items": 5,
    "items": [
      {
        "key": "dashboard_data",
        "size": "2.3MB",
        "created_at": "2025-01-07T10:00:00Z",
        "expires_at": "2025-01-07T11:00:00Z"
      }
    ]
  }
}
```

---

#### 📌 接口11：清空缓存
```
POST /system/cache-clear
```

**请求体**（可选）：
```json
{
  "cache_keys": ["dashboard_data", "trends_data"]
}
```

**响应**：
```json
{
  "code": 200,
  "message": "缓存已清空",
  "data": {
    "cleared_count": 2
  }
}
```

---

#### 📌 接口12：获取系统状态
```
GET /system/status
```

**响应**：
```json
{
  "code": 200,
  "message": "系统状态正常",
  "data": {
    "status": "healthy",
    "version": "1.0.0",
    "uptime": 86400,
    "crawlers_running": 0,
    "db_connected": true,
    "cache_enabled": true,
    "last_crawler_task": {
      "id": "task_xyz",
      "status": "completed",
      "completed_at": "2025-01-07T10:00:00Z"
    }
  }
}
```

---

## 4. 错误码定义

| 错误码 | HTTP状态 | 说明 | 示例 |
|-------|---------|------|------|
| 200 | 200 | 成功 | 数据获取成功 |
| 201 | 201 | 已创建 | 任务创建成功 |
| 202 | 202 | 已接受 | 任务已提交(异步) |
| 400 | 400 | 请求错误 | 参数验证失败 |
| 401 | 401 | 未认证 | 需要登录 |
| 403 | 403 | 禁止 | 无权限访问 |
| 404 | 404 | 未找到 | 资源不存在 |
| 408 | 408 | 请求超时 | 爬虫执行超时 |
| 429 | 429 | 频率限制 | 请求过于频繁 |
| 500 | 500 | 服务器错误 | 数据库连接失败 |
| 503 | 503 | 服务不可用 | 服务维护中 |

---

## 5. 数据交互流程示例

### 流程1：初次加载Dashboard
```
前端                        后端
 │                          │
 ├─ GET /analysis/dashboard─▶
 │                          │ 检查缓存
 │                          │ 如果无缓存，读取本地数据文件
 │                          │ 如果有缓存，返回缓存数据
 │  ◀─ 返回Dashboard数据──┤
 │                          │
 └─ 展示KPI卡片 + 图表
```

### 流程2：执行完整爬虫
```
前端                        后端
 │                          │
 ├─ POST /crawler/start─────▶
 │  (max_pages=10)         │
 │                          │ 创建后台任务
 │  ◀─ task_id + progress──┤ 开始爬虫
 │                          │
 ├─ 轮询 GET /crawler/task/{id}
 │  (每2秒一次)            │
 │  ◀─ progress更新──────┤ (25%, 50%, 75%)
 │                          │
 │                          │ ✅ 爬虫完成
 │  ◀─ completed + data───┤
 │                          │
 └─ 更新全局状态 + 刷新所有图表
```

### 流程3：趋势分析
```
前端                        后端
 │                          │
 ├─ 用户选择时间范围
 │
 ├─ GET /analysis/trends───▶
 │  (?start_date=...&end_date=...&granularity=monthly)
 │                          │ 读取缓存或计算
 │  ◀─ 返回月度数据──────┤
 │                          │
 └─ 绘制趋势折线图
```

---

## 6. 性能优化策略

### 6.1 缓存策略
```
新鲜度要求          缓存时间    策略
────────────────────────────────
Dashboard数据       3600秒      自动更新
趋势分析数据        7200秒      手动更新
用户分析数据        3600秒      自动更新
标签分析数据        7200秒      标签变化时更新
问题列表数据        300秒       分页缓存
```

### 6.2 异步爬虫设计
- **长时间任务**：爬虫自动后台运行
- **进度反馈**：前端轮询或WebSocket更新
- **任务队列**：支持多个爬虫任务排队

### 6.3 数据持久化
```
缓存层:  Redis/内存缓存 (快速访问)
         ↓
数据层:  JSON文件/数据库 (持久存储)
         ↓
源数据:  网站爬虫 (实时更新)
```

---

## 7. 前端集成要点

### 7.1 请求拦截器
```javascript
// 添加通用headers
// 错误统一处理
// 自动重试
// 超时处理
```

### 7.2 响应处理
```javascript
// 解析统一格式
// 错误弹提示
// 加载状态管理
// 数据缓存处理
```

### 7.3 异步任务处理
```javascript
// 轮询Task Status
// 进度条更新
// 任务完成回调
// 错误重试逻辑
```

---

## 总结

✅ **该架构完全可行**，具有以下特点：

1. **清晰的接口规范** - 12个核心API接口
2. **统一的响应格式** - 便于前端解析
3. **异步任务支持** - 长时间爬虫不阻塞
4. **缓存优化** - 减轻后端压力
5. **易于扩展** - 可轻松添加新接口
6. **生产就绪** - 包含错误处理、超时控制等

**下一步**：实现Python后端服务！

